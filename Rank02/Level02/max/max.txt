Assignment name  : max
Expected files   : max.c
Allowed functions:
--------------------------------------------------------------------------------

Write the following function:

int		max(int* tab, unsigned int len);

The first parameter is an array of int, the second is the number of elements in
the array.

The function returns the largest number found in the array.

If the array is empty, the function returns 0.

// primer parametro array de enteros 
//segundo parametro la cntidd de elementos del array 


El primer parámetro es un array de int.

El segundo parámetro es el número de elementos del array.

La función debe devolver el número más grande encontrado en el array.

Si el array está vacío (es decir, si len == 0), la función debe devolver 0.