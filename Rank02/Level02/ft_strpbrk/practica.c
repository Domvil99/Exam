#include <string.h>
#include <stdio.h>

char	*ft_strpbrk(const char *s1, const char *s2)
{   

}

























/*
char	*ft_strpbrk(const char *s1, const char *s2)
{
    while(*s1)
    {
        const char *p = s2;
        while(*p)
        {
            if(*p == *s1)
                return(char *)s1;
            p++;
        }
        s1++;
    }
    return(NULL);
}

int main(void)
{
    char *a = "hola loca";
    char *b = "tremenda p";

    char *uno = strpbrk(a, b);
    //char *dos = ft_strbrk(a, b);

    printf("Cadana A: %s\n Cabedena B: %s\n Uno: %s", a, b, uno);

}
*/